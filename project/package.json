{
  "name": "sql-survival-game",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm run build",
    "dev": "cd frontend && npm start"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
